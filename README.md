# torch-radius-graph

Private CUDA experiment for constructing complete directed periodic cutoff graphs for an entire PyTorch batch.

The repository is under active development. The supported API, reproducible build, correctness evidence, benchmark results, and current limitations will be documented here once the first CUDA path is validated.

